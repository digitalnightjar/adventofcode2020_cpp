set(BINARY ${CMAKE_PROJECT_NAME})
file(GLOB_RECURSE SOURCES LIST_DIRECTORIES true *.hpp *.cpp)
set(SOURCES ${SOURCES})
add_executable(${BINARY}_day03 ${SOURCES})
file(COPY ../data/day03.in DESTINATION ${CMAKE_CURRENT_BINARY_DIR}/data)